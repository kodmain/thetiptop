package mail_test

/*
func TestService(t *testing.T) {

	err := mail.New(nil)
	assert.Error(t, err)

	err = mail.New(&mail.Service{})

	assert.Error(t, err)

	err = mail.New(&mail.Service{
		Host:      "smtp.world.com",
		Port:      "0",
		Username:  "secret",
		Password:  "secret",
		From:      "hello@world.com",
		Expeditor: "Whoami",
	})

	assert.Nil(t, err)

}
*/
